<view class="articleStyle">
    <image class="banner" src="{{article.banner}}" mode="widthFix"/>
    <view class="content">
        <view class="title">{{article.title}}</view>
        <view class="desc">{{article.excerpt}}</view>
    </view>
</view>