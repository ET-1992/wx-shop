<navigator
    wx:for="{{ products }}"
    wx:key="id"
    wx:for-item="product"
    wx:for-index="productIndex"
    hover-class="navigator-hover"
    url="/pages/productDetail/productDetail?id={{ product.id }}"
>
    <view class="productItemSmallCard">
        <image src="{{ product.thumbnail }}" class="productItemImage" mode="aspectFit" />
        <view class="productItemInfo">
            <view class="prodcutItemName">
                {{ product.title }}
            </view>
            <view class="productItemMeta">
                <block wx:if="{{ product.miaosha_enable == 1 && nowTS >= product.miaosha_start_timestamp && nowTS <= product.miaosha_end_timestamp  }}">
                    <view class="productItemPrice">
                        <text>￥</text>{{ product.miaosha_price }}
                    </view>
                </block>
                <block wx:elif="{{ product.groupon_enable == 1}}">
                    <view class="productItemPrice">
                        <text>￥</text>{{ product.groupon_price }}
                    </view>
                </block>
                <block wx:else>
                    <view class="productItemPrice">
                        <text>￥</text>{{ product.price }}
                    </view>
                </block>
                <view class="productItemOriginPrice">
                    <text>￥</text>{{ product.original_price }}
                </view>
                <view class="tagWrapper">
                    <view class="productItemSold" wx:if="{{ product.groupon_enable !== '1' && product.miaosha_enable !== '1' }}">已售：{{ product.sales }}</view>
                    <view class="tag" wx:if="{{ product.groupon_enable === '1' || product.miaosha_enable === '1' }}">
                        {{ product.groupon_enable === '1' ? product.groupon_member_limit + '人团' : '限时购' }}
                    </view>
                </view>
            </view>
        </view>
    </view>
</navigator>
  