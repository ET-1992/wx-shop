<!-- 非虚拟商品和非送礼模式 -->
<view class="deliveryStyle" wx:if="{{ (product.product_type !== 1) && (actions[0].type !== 'onGivingGift') }}" style="--primary-color: {{ themeColor.primaryColor }}">
  <!-- 存在全局快递方式 -->
  <view class="express" wx:if="{{ config.self_lifting_enable || config.home_delivery_enable || config.logistics_enable }}">
    <view class="item-text">配送方式</view>
    <radio-group class="radio-group" bindchange="radioChange">
      <label wx:for="{{ liftStyles }}" wx:key="value">
        <radio value="{{ item.value }}" checked="{{item.checked}}" style="display: none" />
        <view class="title {{ item.checked ? 'title-active' : ''}}">{{ item.text }}</view>
      </label>
    </radio-group>
  </view>
</view>