<view class="container">
	<navigator url="/pages/liftList/liftList?type=4">
		<view class="wrapper" wx:if="{{ addresses[0].name }}" style="{{ isOrderDetail ? 'margin-bottom: 20rpx' : ''}}">
			<view wx:for="{{ addresses }}" wx:key="{{ item.name }}">
				<view class="address-top" wx:if="{{ !isOrderDetail }}">选择门店</view>
				<view class="address-top" wx:if="{{ isOrderDetail }}">所选门店</view>
				<view class="address-part">
					<view class="address-name">{{ item.name }}</view>
					<view class="address-distance" wx:if="{{ !isOrderDetail }}">
						<text>距您 {{ item.distance }} km</text>
						<image src="/icons/right.png" class="right-arrow"></image>
					</view>
				</view>
				<view class="address-info">
					<view class="address-section address-first">
						<view class="address-title">商家地址</view>
						<view class="address-detail">{{item.state}}{{item.city}}{{item.district}}{{item.address}}</view>
					</view>
					<view class="address-section">
						<view class="address-title">营业时间</view>
						<view class="address-time">{{ item.time[0] }} - {{ item.time[1] }}</view>
					</view>
					<view class="address-section address-second">
						<view class="address-title">商家电话</view>
						<view class="address-phone">{{ item.phone }}</view>
					</view>
				</view>
			</view>
		</view>

		<view class="storeList-section" wx:else>
			<view class="address title">选择门店</view>
			<image src="/icons/right.png" class="right-arrow"></image>
		</view>
	</navigator>
</view>
