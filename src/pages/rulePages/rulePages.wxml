<loading isLoading="{{ isLoading }}"/>
<view class="rule-wrap"  wx:if="{{ !isLoading }}">
	<image class="rule-image" src="{{ image_url }}" mode="widthFix" wx:if="{{ image_url }}" />
	<view class="noDataText" wx:if="{{ !image_url }}">暂无规则详情</view>
</view>