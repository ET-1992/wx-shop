
// 计算到账金额
var getIncomeAmount = function (price, weight, loss){
    var res = price * weight * (100-loss) * 0.01 || 0;
    res = res.toFixed(2);
    return res;
};

module.exports = {
    getIncomeAmount: getIncomeAmount,
}